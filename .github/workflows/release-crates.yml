name: Publish Crates

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths:
      - '**/Cargo.toml'
      - '.github/workflows/release-crates.yml'
      - '.github/actions/publish-crate/**'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  # Tier 1: Crates with no internal dependencies
  tier1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - crate: openai-protocol
            path: crates/protocols
          - crate: reasoning-parser
            path: crates/reasoning_parser
          - crate: kv-index
            path: crates/kv_index
          - crate: data-connector
            path: crates/data_connector
          - crate: wfaas
            path: crates/workflow
          - crate: smg-auth
            path: crates/auth
          - crate: smg-mcp
            path: crates/mcp
          - crate: smg-mm-rdma
            path: crates/mm_rdma
          - crate: engine-zmq-client
            path: crates/engine_zmq_client
          - crate: smg-radix-tree
            path: crates/radix_tree
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/publish-crate
        with:
          crate: ${{ matrix.crate }}
          path: ${{ matrix.path }}
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # Tier 2: Crates depending on tier 1
  tier2:
    needs: tier1
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - crate: tool-parser
            path: crates/tool_parser
          - crate: smg-grpc-client
            path: crates/grpc_client
          - crate: smg-client
            path: clients/rust
          - crate: smg-mesh
            path: crates/mesh
          - crate: smg-wasm
            path: crates/wasm
          - crate: llm-tokenizer
            path: crates/tokenizer
          - crate: smg-rl
            path: crates/rl
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/publish-crate
        with:
          crate: ${{ matrix.crate }}
          path: ${{ matrix.path }}
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # Tier 3: Crates depending on tier 2
  tier3:
    needs: tier2
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - crate: llm-multimodal
            path: crates/multimodal
          # radix-index: the shared prefix-cache index service. Depends on
          # radix-tree (tier 1) and smg-grpc-client (tier 2, event bridge).
          - crate: smg-radix-index
            path: crates/radix_index
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/publish-crate
        with:
          crate: ${{ matrix.crate }}
          path: ${{ matrix.path }}
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # Tier 4: Main crate depending on all others
  tier4:
    needs: tier3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/publish-crate
        with:
          crate: smg
          path: model_gateway
          token: ${{ secrets.CARGO_REGISTRY_TOKEN }}

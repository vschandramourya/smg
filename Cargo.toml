[workspace]
members = ["model_gateway", "crates/protocols", "crates/reasoning_parser", "crates/tool_parser", "crates/workflow", "crates/tokenizer", "crates/auth", "crates/mcp",
    "crates/external_router", "crates/kv_index", "crates/data_connector", "crates/multimodal", "crates/mm_rdma", "crates/wasm", "crates/mesh", "crates/grpc_client", "crates/engine_zmq_client", "bindings/python", "bindings/golang", "clients/rust", "clients/openapi-gen", "crates/mock_worker", "crates/rl", "crates/radix_tree", "crates/radix_index"]
resolver = "2"

[workspace.dependencies]
# Internal workspace crates
openai-protocol = { version = "1.13.0", path = "crates/protocols" }
smg-mm-rdma = { version = "0.1.1", path = "crates/mm_rdma" }
reasoning-parser = { version = "1.7.0", path = "crates/reasoning_parser" }
tool-parser = { version = "1.7.0", path = "crates/tool_parser" }
wfaas = { version = "1.1.0", path = "crates/workflow" }
llm-tokenizer = { version = "1.7.0", path = "crates/tokenizer" }
smg-auth = { version = "1.2.3", path = "crates/auth" }
smg-mcp = { version = "2.3.3", path = "crates/mcp" }
smg-external-router = { version = "0.1.0", path = "crates/external_router" }
kv-index = { version = "1.4.0", path = "crates/kv_index" }
smg-radix-tree = { version = "0.1.0", path = "crates/radix_tree" }
smg-radix-index = { version = "0.1.0", path = "crates/radix_index" }
smg-data-connector = { version = "2.3.4", path = "crates/data_connector", package = "data-connector" }
llm-multimodal = { version = "1.11.0", path = "crates/multimodal" }
smg-wasm = { version = "1.1.4", path = "crates/wasm", package = "smg-wasm" }
smg-mesh = { version = "1.4.3", path = "crates/mesh", package = "smg-mesh" }
smg-grpc-client = { version = "1.12.0", path = "crates/grpc_client" }
engine-zmq-client = { version = "0.1.0", path = "crates/engine_zmq_client" }
smg-rl = { version = "0.1.0", path = "crates/rl" }

# Shared dependencies
anyhow = "1.0"
async-trait = "0.1"
futures = "0.3"
prost = "0.14.4"
prost-types = "0.14.4"
tonic = { version = "0.14.6", features = ["gzip", "transport"] }
tonic-prost = "0.14.6"
axum = { version = "0.8.9" }
blake3 = "1.8"
lz4_flex = "0.14"
bytemuck = { version = "1.25" }
byteorder = "1.5.0"
bytes = "1.12.0"
half = { version = "2.7.1", features = ["bytemuck"] }
rmp-serde = "1.3.1"
rmpv = { version = "1.3.1", features = ["with-serde"] }
serde_default = "0.2.0"
serde_repr = "0.1.20"
serde_tuple = "1.1.3"
zeromq = { version = "0.6.0", default-features = false, features = ["tokio-runtime", "all-transport"] }
chrono = { version = "0.4" }
dashmap = "6.2.1"
tikv-jemallocator = { version = "0.6", default-features = false, features = ["background_threads_runtime_support"] }
tikv-jemalloc-ctl = { version = "0.6", default-features = false }
http = "1.4.2"
lru = "0.18.0"
num-traits = "0.2"
parking_lot = "0.12.5"
rand = "0.10.1"
reqwest = { version = "0.13.4", default-features = false }
serde = { version = "1.0" }
serde_json = { version = "1.0", features = ["preserve_order"] }
serde_with = { version = "3", features = ["macros"] }
subtle = "2.6"
thiserror = "2.0.18"
tokio = { version = "1.52.3" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "chrono"] }
uuid = { version = "1.23", features = ["v7", "serde"] }
wasmtime = { version = "45.0", features = ["component-model", "async"] }
wasmtime-wasi = "45.0"
tokio-util = { version = "0.7", features = ["rt"] }
tokio-tungstenite = { version = "0.30", features = ["rustls-tls-native-roots"] }
webpki-roots = "1.0"
multer = "3"
str0m = { version = "0.23", default-features = false, features = ["openssl"] }
scopeguard = "1.2"
bitflags = "2.13.0"
schemars = "1.2"

[workspace.lints.rust]
unsafe_code = "deny"
unused_qualifications = "warn"

[workspace.lints.clippy]
# Deny unwrap in production code (tests exempted via clippy.toml)
unwrap_used = "deny"
# Warn for now — promote to deny after cleanup (99 expect, 82 panic violations)
expect_used = "warn"
panic = "warn"

# Deny debug/placeholder macros (currently zero violations)
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
unreachable = "deny"

# Print macros — use tracing instead (58 existing violations, warn for now)
print_stdout = "warn"
print_stderr = "warn"

# Enforce #[expect] over #[allow] for better lint tracking (92 existing violations, warn for now)
allow_attributes = "warn"

# Disallowed methods (see clippy.toml for the list) — warn for now, promote after audit
disallowed_methods = "warn"

# Code quality lints — warn initially, promote to deny after cleanup
explicit_iter_loop = "warn"
explicit_into_iter_loop = "warn"
semicolon_if_nothing_returned = "warn"
uninlined_format_args = "warn"
manual_assert = "warn"
manual_string_new = "warn"
inconsistent_struct_constructor = "warn"
needless_raw_string_hashes = "warn"
cloned_instead_of_copied = "warn"
inefficient_to_string = "warn"
assigning_clones = "warn"
stable_sort_primitive = "warn"
unnecessary_wraps = "warn"
unused_self = "warn"
unused_async = "warn"
if_not_else = "warn"
match_bool = "warn"
match_wildcard_for_single_variants = "warn"
trivially_copy_pass_by_ref = "warn"
or_fun_call = "warn"
ignored_unit_patterns = "warn"
ref_option = "warn"
large_futures = "warn"

# Import hygiene
single_component_path_imports = "warn"
absolute_paths = "warn"

# These pedantic lints are explicitly allowed to give authors discretion
default_trait_access = "allow"
match_same_arms = "allow"
must_use_candidate = "allow"
single_match_else = "allow"
doc_lazy_continuation = "allow"

[profile.release]
opt-level = "z"     # Optimize for size
lto = "fat"         # Full LTO for smaller binaries
codegen-units = 1   # Better optimization, slower compile
strip = false       # Keep symbols so production CPU profiles/flamegraphs are readable (#1575)

# Optimize hot dependencies in the tiktoken encode path.
[profile.release.package.tiktoken-rs]
opt-level = 2

[profile.release.package.fancy-regex]
opt-level = 2

[profile.release.package.regex-automata]
opt-level = 2

[profile.ci]
inherits = "release"
opt-level = 2       # Lighter optimization (still fast runtime, much faster compile)
lto = "thin"        # Thin LTO - good balance
codegen-units = 16  # More parallelization for faster builds
strip = true

[profile.dev]
opt-level = 0
debug = 1
split-debuginfo = "unpacked"
incremental = true
codegen-units = 256

[profile.dev.package."*"]
opt-level = 2
debug = false


[profile.dev.build-override]
opt-level = 3
codegen-units = 1

[profile.dev-opt]
inherits = "dev"
opt-level = 1

[profile.bench]
opt-level = 3       # Max speed for benchmarks (release uses "z" for size)
lto = "thin"        # Good balance of speed and compile time
codegen-units = 1   # Better optimization
